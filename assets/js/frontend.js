var News_Manager = {
				'cache': {},
				'animSpeed': 'medium'
}; //Init the primary frotend object

(function($) {
				$(document).ready(function(){
								News_Manager.init_dom_cache();
								News_Manager.bind_events();
				});
				
				/**********************************************************************
					* Caches DOM elements for faster access
					*********************************************************************/
				News_Manager.init_dom_cache = function(){
								
				}
				
				/**********************************************************************
					* Bind events to the frontend page's controls
					*********************************************************************/
				News_Manager.bind_events = function(){
								
				}
}(jQuery));